# Project Setup

Run the following commands to install all dependencies and start the development server:

npm install uikit  
npm install  
npm run dev

This will install UIkit, fetch all required dependencies, and run the web application locally.

# Click the localhost link to view our finished version
